
// #1 code challenge

function checkPal(word) {
const originalWord = word.toUpperCase()
const worldLength = word.length
//reverse order
console.log(backwardsWord, backardsWord)
console.log(word, originalWord === backwardsWord)
return originalWord === backwardsWord
}
let backwardsWord =''
for (let i = wordLength - 1; i >= 0; i--){
    backwardsWor += originalWold[i]
} 

checkPal('Wow')
checkPal('Dennis')